{
    "name": "reveal-md-test",
    "version": "1.0.0",
    "scripts": {
        "build": "rm -rf ./docs/ && mkdir ./docs && reveal-md slides/ --static docs"
    },
    "devDependencies": {
        "reveal-md": "^4.3.0"
    }
}
